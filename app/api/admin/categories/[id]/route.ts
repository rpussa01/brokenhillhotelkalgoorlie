import { NextResponse } from "next/server";
import { isAdmin } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!(await isAdmin()))return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params;const changes=await request.json();const data:{name?:string;active?:boolean;sortOrder?:number}={};if(typeof changes.name==="string"){const name=changes.name.trim();if(name.length<2)return NextResponse.json({error:"Enter a valid category name."},{status:400});data.name=name;}if(typeof changes.active==="boolean")data.active=changes.active;if(Number.isInteger(changes.sortOrder))data.sortOrder=changes.sortOrder;try{return NextResponse.json(await prisma.category.update({where:{id},data}));}catch{return NextResponse.json({error:"Category not found or name already exists."},{status:404});}}
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){if(!(await isAdmin()))return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params;const count=await prisma.menuItem.count({where:{categoryId:id}});if(count>0)return NextResponse.json({error:"Delete or move all items in this category first."},{status:409});try{await prisma.category.delete({where:{id}});return NextResponse.json({ok:true});}catch{return NextResponse.json({error:"Category not found."},{status:404});}}
